---
all:
  vars:
    ansible_python_interpreter: "python"
  children:
    zookeeper:
      hosts:
    kafka:
      hosts:
    test:
      hosts:
        localhost:
          ansible_connection: local
